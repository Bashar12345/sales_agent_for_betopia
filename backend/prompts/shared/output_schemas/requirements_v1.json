{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "requirements_v1",
  "title": "RequirementsExtraction",
  "description": "Structured output schema for P2 Requirements Agent extraction",
  "type": "object",
  "required": ["service_type", "features", "raw_summary", "extraction_confidence"],
  "properties": {
    "service_type": {
      "type": "string",
      "enum": [
        "web_development", "mobile_app", "api_integration",
        "data_analytics", "ai_ml", "design", "consulting", "other"
      ]
    },
    "platform": {
      "type": "string",
      "enum": ["web", "ios", "android", "cross_platform", "backend_only", "desktop", "not_specified"]
    },
    "features": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of specific features/requirements extracted"
    },
    "tech_preferences": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Technologies mentioned or implied by the client"
    },
    "budget_min": {
      "type": ["number", "null"],
      "description": "Minimum budget in USD if inferable"
    },
    "budget_max": {
      "type": ["number", "null"],
      "description": "Maximum budget in USD if inferable"
    },
    "deadline_description": {
      "type": ["string", "null"],
      "description": "Raw deadline text as stated by client"
    },
    "raw_summary": {
      "type": "string",
      "description": "Free-text summary of all extracted requirements"
    },
    "extraction_confidence": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Model confidence in the extraction quality"
    }
  }
}
